

singleton Material(leaves_strong)
{
    mapTo = "leaves_strong";
    groundType = "leaves_strong";
    groundDepth = 0.6;
};

singleton Material(leaves_thin)
{
    mapTo = "leaves_thin";
    groundType = "leaves_thin";
    groundDepth = 1;
};